Built with Go

To build:

    export GOPATH=[...]/git/epaxos/

    go install ppmaster
    go install server
    go install client

To run:

    bin/master &
    bin/server -port 7070 &
    bin/server -port 7071 &
    bin/server -port 7072 &
    bin/client
