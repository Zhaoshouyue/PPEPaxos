Built with Go

To build:

    go install ppmaster
    go install server
    go install clientlat

To run:

    bin/master &
    bin/server -ppe=true &
    bin/server -ppe=true &
    bin/server -ppe=true &
    bin/clientlat
