Built with Go

To build:

    go install master
    go install server
    go install client

To run:

    bin/master &
    bin/server -ppe=true &
    bin/server -ppe=true &
    bin/server -ppe=true &
    bin/client
