Built with Go

To build:

    export GOPATH=[...]/git/epaxos/

    go install master
    go install server
    go install client

To run:

    bin/master &
    bin/server -ppe=true &
    bin/server -ppe=true &
    bin/server -ppe=true &
    bin/client
